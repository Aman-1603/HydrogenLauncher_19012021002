<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_main"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Setting_Modual"
    android:padding="40dp">

    <Button
        android:id="@+id/setting_brightness_down"
        android:layout_width="120dp"
        android:layout_height="80dp"
        android:background="@drawable/setting_back_notification"
        android:text="Down brightness"/>

    <Button
        android:id="@+id/setting_brightness_up"
        android:layout_width="120dp"
        android:layout_height="80dp"
        android:layout_marginLeft="10dp"
        android:background="@drawable/setting_back_notification"
        android:text="UP brightness"/>

</LinearLayout>